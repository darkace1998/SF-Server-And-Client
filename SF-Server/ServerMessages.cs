namespace SFServer;

/// <summary>
/// Contains server message strings for localization.
/// </summary>
public static class ServerMessages
{
    public const string RemovedOldConnection = "Removed old connection to allow new Steam ID from same IP";
    public const string ServerFull = "Server is full, cannot add new client";
    public const string AddedNewClient = "Added new client at index {0}!";
    public const string ClientRemoved = "Client removed at index: {0}";
}
